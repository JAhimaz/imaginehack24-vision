# VISION - HR KPI Prediction Dashboard

You are required to run both the server / client

Client
1. Enter into Client folder
2. Run `yarn install`
3. Run `yarn dev`

Server
1. Enter into Server folder
2. Run `yarn install`
3. Run `yarn dev`